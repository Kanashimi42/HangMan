#pragma once
using namespace std;
#include "game.h"
#include<iostream>
#include<string.h>

class resultprint
{
	void print_status(game)
	{
		// добавить возможность просмотра угаданых слов по виду * a * * m a * (hangman)	cout << "/nWord: " << endl;
		// Добавить возможность просмотра ошибочных букв в виде "Errors: 4, a b c d	cout << "Errors: " << errors << ", " << bad_letters << endl;
		/*	if (errors >= 7) каждую ошибку отрисовывать новый рисунок виселицы!
			{
				cout << "You lost!" << endl;

			}
			else {
				if (letters.size() == right_letters.size)
				{
					cout << "You win!" << endl; возможно поменять рисунок после победы??
				}
				else
				{

				}
			}
		не забыть добавить очистку экрана!!
		*/
	}
};

